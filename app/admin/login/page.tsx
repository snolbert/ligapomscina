"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { signInAdmin } from "@/lib/admin"; import { Panel } from "@/components/ui/panel"; import { Button } from "@/components/ui/button";
export default function AdminLoginPage() { const router = useRouter(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); return <main className="flex min-h-screen items-center justify-center bg-abyss px-4"><Panel className="w-full max-w-md p-8"><p className="font-display text-3xl text-gold">Logowanie admina</p><p className="mt-2 text-sm text-zinc-400">Użyj konta Supabase Auth z rolą administratora.</p><form className="mt-6 grid gap-4" onSubmit={async (e) => { e.preventDefault(); const { error } = await signInAdmin(email, password); if (error) return alert(error.message); router.push("/admin"); router.refresh(); }}><input value={email} onChange={(e) => setEmail(e.target.value)} placeholder="E-mail" className="rounded-2xl border border-gold/20 bg-black/20 px-4 py-3" /><input value={password} onChange={(e) => setPassword(e.target.value)} type="password" placeholder="Hasło" className="rounded-2xl border border-gold/20 bg-black/20 px-4 py-3" /><Button type="submit">Zaloguj</Button></form></Panel></main>; }
