import { notFound } from "next/navigation"; import { Panel } from "@/components/ui/panel"; import { getNewsBySlug } from "@/lib/queries"; import { formatDate } from "@/lib/utils";
export default async function Page({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const news = await getNewsBySlug(slug); if (!news) return notFound(); return <main className="mx-auto max-w-4xl px-4 py-10 md:px-6 md:py-16"><Panel className="overflow-hidden"><div className="aspect-[16/8] overflow-hidden"><img src={news.image_url || "/assets/backgrounds/news-placeholder.jpg"} alt={news.title} className="h-full w-full object-cover" /></div><div className="space-y-5 p-8"><p className="text-xs uppercase tracking-[0.25em] text-gold/70">{formatDate(news.published_at)}</p><h1 className="font-display text-4xl text-gold">{news.title}</h1><p className="text-lg text-zinc-200">{news.lead}</p><div className="prose-fantasy whitespace-pre-wrap text-sm leading-7">{news.content}</div></div></Panel></main>; }
