import { SectionHeading } from "@/components/ui/section-heading"; import { Panel } from "@/components/ui/panel"; import { getRules } from "@/lib/queries";
export default async function Page() { const rules = await getRules(); return <main className="mx-auto max-w-5xl space-y-8 px-4 py-10 md:px-6 md:py-16"><SectionHeading title="Zasady ligi" description="Regulamin rozgrywek Survival Chaos i Legion TD." /><div className="space-y-6">{rules.map((rule: any) => <Panel key={rule.id} className="p-6"><h2 className="font-display text-2xl text-gold">{rule.title}</h2><div className="prose-fantasy mt-4 whitespace-pre-wrap text-sm leading-7">{rule.content}</div></Panel>)}</div></main>; }
