import { MatchCard } from "@/components/site/match-card"; import { SectionHeading } from "@/components/ui/section-heading"; import { getCurrentSeason, getMatches } from "@/lib/queries";
export default async function Page() { const season = await getCurrentSeason(); const matches = await getMatches({ seasonId: season?.id }); return <main className="mx-auto max-w-7xl space-y-8 px-4 py-10 md:px-6 md:py-16"><SectionHeading title="Wyniki" description="Rozegrane mecze, MVP, komentarze i szczegóły uczestników." /><div className="grid gap-6 lg:grid-cols-2">{matches.filter((m: any) => m.status === "played").map((match: any) => <MatchCard key={match.id} match={match} />)}</div></main>; }
