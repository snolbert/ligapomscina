import Link from "next/link"; import { SectionHeading } from "@/components/ui/section-heading"; import { Panel } from "@/components/ui/panel"; import { getSeasons } from "@/lib/queries";
export default async function Page() { const seasons = await getSeasons(); return <main className="mx-auto max-w-7xl space-y-8 px-4 py-10 md:px-6 md:py-16"><SectionHeading title="Archiwum sezonów" description="Pięć sezonów ligi, w tym aktualny i historyczne rozstrzygnięcia." /><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{seasons.map((season: any) => <Link key={season.id} href={`/archiwum/${season.slug}`}><Panel className="h-full p-6 transition hover:-translate-y-1 hover:shadow-glow"><p className="font-display text-3xl text-gold">{season.name}</p><p className="mt-3 text-sm text-zinc-300">{season.archived_summary || "Zobacz tabelę, wyniki i podsumowanie sezonu."}</p></Panel></Link>)}</div></main>; }
