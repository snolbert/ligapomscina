import { SectionHeading } from "@/components/ui/section-heading"; import { StandingsTable } from "@/components/site/standings-table"; import { getCurrentSeason, getStandings } from "@/lib/queries";
export default async function TabelaPage() { const season = await getCurrentSeason(); const standings = season ? await getStandings(season.id) : []; return <main className="mx-auto max-w-7xl space-y-8 px-4 py-10 md:px-6 md:py-16"><SectionHeading eyebrow="Sezon bieżący" title="Tabela ligowa" description="Zbiorcza punktacja z trybów Survival Chaos i Legion TD." /><StandingsTable rows={standings} /></main>; }
